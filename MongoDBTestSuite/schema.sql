
CREATE DATABASE IF NOT EXISTS dbname ;
USE dbname;

CREATE TABLE a (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE b (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE c (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE d (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE e (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE f (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE g (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE h (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE i (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE j (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE k (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE l (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE m (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE n (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE o (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE p (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE q (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE r (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE s (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE t (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE u (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE v (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE w (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE x (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE y (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);
CREATE TABLE z (
    listing_id int PRIMARY KEY AUTO_INCREMENT,
    title varChar(50)
);